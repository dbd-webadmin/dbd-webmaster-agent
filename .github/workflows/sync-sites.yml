name: Sync Sites from Google Sheets

on:
  schedule:
    - cron: '6 6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Sync sites from Google Sheets
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: node scripts/sync-sites.js

      - name: Commit updated sites.json
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add sites.json
          git diff --staged --quiet || git commit -m "Sync sites from Google Sheets $(date -u +'%Y-%m-%d')"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main
            git push && break
            echo "Push rejected (attempt $i), retrying..."
            sleep $(( (RANDOM % 10) + 1 ))
          done
